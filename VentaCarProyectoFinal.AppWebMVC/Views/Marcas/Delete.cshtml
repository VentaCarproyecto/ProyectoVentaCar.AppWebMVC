@model VentaCarProyectoFinal.AppWebMVC.Models.Marca

@{
    ViewData["Title"] = "Delete";
}

<h1 class="display-4" style="font-family:'Cambria Math'">Eliminar Marca</h1>


<h3>Estas Seguro que quieres eliminar esta Marca?</h3>
<div>

    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Marca1)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Marca1)
        </dd>

    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />

        <div class="d-flex justify-content-end gap-2 mt-4">
            <input type="submit" value="Eliminar" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</div>

